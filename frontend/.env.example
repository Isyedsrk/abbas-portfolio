# Optional: copy to .env.production for local `npm run build` testing.
# Netlify (Git): Site settings → Environment variables, or netlify.toml [build.environment].
# Netlify Drop: run `npm run build:drop` (embeds this URL); then drag `dist` onto Netlify Drop.
VITE_API_URL=https://abbas-portfolio-1sj5.onrender.com
