# Optional: copy to .env.production for local `npm run build` testing.
# Netlify: also set in Site settings → Environment variables, or use netlify.toml [build.environment].
VITE_API_URL=https://abbas-portfolio-1sj5.onrender.com
